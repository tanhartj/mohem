# ⚡ راه‌اندازی سریع (5 دقیقه)

## 🎯 سریع‌ترین روش برای شروع

### گام 1: نصب Redis (30 ثانیه)
```bash
docker run -d -p 6379:6379 redis:7-alpine
```

### گام 2: نصب و راه‌اندازی (2 دقیقه)
```bash
# نصب پکیج‌ها
npm install

# تنظیم .env
cp .env.example .env

# راه‌اندازی database
npm run init-db

# اجرا
npm start
```

### گام 3: دسترسی (10 ثانیه)
```
🌐 Dashboard: http://localhost:5000
✅ Health: http://localhost:5000/health
```

---

## ✅ همین!

پروژه شما اکنون در حال اجراست!

### بعدی چه کار کنم؟

1. **اتصال YouTube** (اختیاری):
   - برو به http://localhost:5000/auth/youtube
   - با حساب Google وارد شو

2. **API Keys اضافه کن** (اختیاری):
   - فایل `.env` را ویرایش کن
   - OpenAI, YouTube, Telegram keys اضافه کن

3. **شروع تولید ویدیو**:
   - بدون API keys: از Fallback Generator استفاده می‌شود
   - با API keys: تولید محتوای AI

---

## 🚨 مشکل داری؟

### Redis متصل نمی‌شود:
```bash
docker ps  # چک کن که Redis در حال اجراست
```

### Port 5000 اشغال است:
در `.env`:
```
PORT=3000
```

---

## 📖 اطلاعات بیشتر

- راهنمای کامل: `DEPLOY_INSTRUCTIONS_FA.md`
- نصب روی Render: `RENDER_DEPLOY.md`
- گزارش بررسی: `REPORT-FA.md`

**موفق باشید! 🎬**
